export const dataBackupService = {
  // Reserved for future local backup workflows.
  runManualBackup(): { ok: boolean; message: string } {
    return { ok: true, message: "Backup service placeholder." };
  }
};

